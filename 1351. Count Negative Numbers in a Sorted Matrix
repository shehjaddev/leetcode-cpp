class Solution {
public:
    int countNegatives(vector<vector<int>>& grid) {
        int count = 0;

        for(auto g: grid) {
            for(int n: g) {
                if (n < 0) count++;
            }
        }

        return count;
    }
};
