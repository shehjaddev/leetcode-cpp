class Solution {
public:
    vector<int> getConcatenation(vector<int>& nums) {
        vector<int> ans;
        int t = 2;

        while(t--) {
            for (auto num: nums) {
                ans.push_back(num);
            }
        }

        return ans;
    }
};
