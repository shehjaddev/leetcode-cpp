class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        vector<int> result;

        for (auto x: nums) {
            result.push_back(nums[x]);
        }

        return result;
    }
};
